# 每日台股當沖推薦報告 — 雲端自動生成版

你在一個全新的雲端沙盒環境執行，**沒有瀏覽器、沒有 Playwright**。只能用 Bash（curl、node）、WebFetch、WebSearch。這份文件已經內建完整方法論，不用參考其他地方。

執行完直接把結果寫進本 repo 的 `data/latest.json`，然後 commit + push 到 `main` branch，不用問任何人、不用解釋過程。

**最高原則：資料品質比看起來完整更重要。** 任何一步如果你無法確認資料完整或正確（例如抓不到全部分頁、查不到某檔股票的處置股狀態、國際盤勢時間點有時差疑慮），就把該項寫進 `data_quality.warnings`（見下方 JSON 格式），並在對應的選股/結論上誠實降級信心度，不要假裝資料完整。寧可交出一份「標了警示但誠實」的報告，也不要交出一份「看起來完整但其實漏東西」的報告。

**雙來源查證原則（2026-08-06 新增，使用者明確要求）：任何會寫進 `narrative_timeline`、`sentiment.factors` 的具體因果論述或數字**（例如「投信買超延續 X 個交易日」「某產業訂單從中國轉單到台灣」「法人喊多/喊空」這類敘事型內容，不是單純的收盤價這種一次查證就能確定的硬數字）——這類內容比股價更容易查到單一來源、過時、或帶立場的說法，**一律至少用兩個不同來源（不同網域、不同發布單位，同一篇新聞被多家媒體轉貼不算兩個來源）交叉確認內容一致，才能當作確定事實寫進去**：
- 兩個以上來源數字/說法一致：正常寫入，不用特別標注。
- 只查到一個來源、或多個來源數字兜不起來：**不能挑一個看起來最合理的直接當事實寫**，改成在該句後面加註「（單一來源，未交叉查證）」或「（來源數字不一致：A 來源 x%／B 來源 y%，取較保守值）」，如果不確定到連保守寫都有風險，寧可這次不寫這個節點，也不要硬湊一則查證不足的敘事。
- 這條規則不是要你每個數字都查到天荒地老——查證到兩個獨立來源說法一致就可以停，不用無限查下去；重點是「有沒有交叉比對過」，不是「查了幾十次」。

## 第 0 步：確認今天是不是正常交易日（2026-07-16 新增，回測抓到的真實事故）

**先講事故**：2026-07-10 因颱風巴威來襲，證交所全日休市（公告前一晚 7/9 20:32 就已發出），但那天的報告完全沒查證這件事，照樣生出一組進場/停利/停損價位——等於寫給一個不存在的交易日，使用者拿到手上完全沒用。

**這一步必須放在所有步驟最前面**：用 WebSearch 查「台股 今日 是否休市」或直接查證交所公告的休市日期列表（颱風假、國定假日、補行上班日調整等），確認今天（`date` 欄位對應的日期）是不是正常的交易日。

- **確認是正常交易日**：往下照第 1 步開始走，正常流程。
- **確認今天休市**（颱風假、國定假日等）：**不要生出一般的進場/停利/停損建議**。改寫成這個簡化版 JSON（其他欄位可以省略）：
  ```json
  {
    "date": "2026-07-10",
    "generated_at": "2026-07-10 08:30",
    "provisional": false,
    "market_open": false,
    "market_closed_reason": "颱風巴威來襲，證交所公告全日休市",
    "data_quality": { "warnings": [] },
    "summary": {},
    "candidates": [],
    "news": []
  }
  ```
  直接 commit push 這個版本，不用往下跑第 1-9 步。
- **查不到明確答案、無法確認今天是否正常交易**：當作正常交易日繼續走完整流程，但在 `data_quality.warnings` 誠實標注「本次未能 100% 確認今天是否為正常交易日，若當天實際休市，以下建議無效」。

## 第 1 步：抓成交量排行

前往 TAIFEX 行情網（mis.taifex.com.tw）「一般交易時段行情 → 股票類契約報價」，篩選到期月份為近月合約。

**這個網站的已知陷阱**（本機用 Playwright 手動操作時踩過）：
- 頁面右下角「熱門股票期貨」快速連結區塊的清單**跟真正的成交量排行不是同一份**，不要用。
- 網站的表格「點擊排序」只在當前已載入的那一頁內排序，不是跨全部資料排序。近月合約通常有 297 筆、分 3 頁（每頁 100 筆），排在後面分頁的高量股會被漏掉。

**你在雲端沒有瀏覽器，做法改成**：
1. 用 `curl` 直接抓這個頁面（或它背後實際回傳資料的 API/AJAX 端點——用 `curl -v` 先看回應是完整 HTML 表格還是需要額外 POST 參數）。如果是 server-side render 的 HTML 表格，直接用 `node` 寫一段腳本解析 HTML（正則或簡單字串處理抓「商品」「成交量」欄位），不要交給 WebFetch 的摘要模型（大量表格資料容易被摘要模型漏抓或幻覺，之前在解析 Yahoo Finance 分時資料時就出過這個問題）。
2. 如果頁面需要分頁參數（例如 `?page=2`），逐頁抓取，並且**確認你抓到的總筆數/總頁數，跟頁面上顯示的「共 N 筆」或「第 X 頁，共 Y 頁」一致**。不一致就是抓漏了，要繼續抓或在 warnings 註明「TAIFEX 成交量排行可能未抓取完整（只取得 X/Y 頁）」。
3. 如果 curl 抓不到可用資料，退而求其次用 WebFetch 抓這個頁面，但**明確要求 WebFetch 逐行列出所有看到的「商品」與「成交量」，不要摘要**，並且事後核對數量是否合理（近月通常 250-300 檔）；數量明顯偏少（例如只有十幾檔）就代表被摘要模型截斷了，要在 warnings 註明「成交量排行透過 WebFetch 取得，可能不完整，建議人工複核」。

**4. 如果 curl 跟 WebFetch 對 TWSE/TAIFEX 這類網域全部回傳 403（gateway 政策拒絕）——這是已經連續發生兩週以上的已知常態，不是偶發斷線，不用浪費時間反覆重試確認「是不是真的被擋」，直接進入這一步**（2026-07-28 新增，回測發現候選池從 7 檔一路退化到 3 檔的根因就是每天各自臨場亂想替代法，這裡把已驗證有效的做法固定下來，不要再各自發明）：
   - **用至少 6-8 組不同角度的 WebSearch 查詢去拼湊出當天的熱門/高量股名單**，例如：「台股 [日期] 成交量排行」「台股期貨 [日期] 成交量前20」「[日期] 台股 大漲 個股」「[日期] 台股 大跌 個股」「[日期] 台股 法人買超」「[日期] 台股期貨 熱門 契約」，盡量涵蓋漲、跌、量三個角度，不要只搜「今天大漲的股票」這種單一角度（那樣會漏掉高量但平盤的股票，也是候選池一直縮水的原因之一）。
   - **目標是拼出至少 8-12 檔候選**，明顯少於這個數字（例如只有 3-5 檔）代表查詢角度不夠多元，要再補查，不是查到什麼算什麼就收工。
   - 對每一檔查到的股票，優先確認是否有對應的個股期貨合約（可用 WebSearch 查「[股票代號] 期貨」，有期貨合約掛牌才符合方法論篩選的族群），沒有把握的中小型股/妖股型態的標的可以排除，但排除後的候選池仍要盡量補到 8-12 檔的水準，不要排除完就直接收工變成 3 檔。
   - 這個替代法本身仍然是降級後的方法論（不是真的成交量排行），要在 `data_quality.warnings` 誠實記錄，但**候選池大小本身也要顧到，不能因為降級就順便縮水**。
   - **查詢上限（2026-08-06 新增）：最多花 20 組 WebSearch 查詢拼湊候選池。到了 20 組還是湊不到 8-12 檔，就用查到的實際數量（哪怕只有 5-8 檔）往下走完整個流程，在 `data_quality.warnings` 誠實記錄「候選池只拼到 X 檔，未達 8-12 檔目標」，不要為了硬湊到目標檔數而無限查下去、把時間都耗在這一步，導致後面第 5-9 步沒時間做完。

## 第 2 步：去重、轉換成股票

第一頁商品名稱是「XX期貨」，把同一支股票的重複月份合約去重，去掉「期貨」兩字就是對應股票代號。

用 WebSearch 或直接查證券交易所公開資訊（例如查「[股票代號] 收盤價」）取得每支股票現貨的收盤價、漲跌幅、振幅、開高低——這是股票本身的資料，不是期貨合約的資料。

## 第 3 步：篩選

依漲跌幅「絕對值」排序（不分正負），拿掉波動最小的 2-3 支。

## 第 4 步：排行與分組

剩下的股票依波動幅度排序給排行，每一名附入選理由。檢查是否有龍頭股（例如聯電之於晶圓代工），有的話把同產業/供應鏈相關族群整理在一起分組呈現。

## 第 5 步：每檔給完整交易計畫

**5a. 先查已知催化劑（2026-07-16 新增，回測抓到的真實模式）**：對每一檔候選，用 WebSearch 查「[股票名稱] 法說會」或「[股票名稱] 股東會 重大訊息」，確認近期（尤其未來 1-2 個交易日內）有沒有排定法說會、重大公告、除權息這類已知事件。回測發現南亞科（7/9）、大立光（7/13→7/14）的兩次最大跳空，背後都是法說會前後的分析師目標價劇動——這種事件的日期本身是公開排定的資訊，查得到。
- **有排定近期事件**：這一檔的進場區間精確度視為偏低，在該檔說明文字或 `risk_tag` 明確標注「近期有法說會/重大事件，跳空風險偏高，進場區間僅供參考」，不要照樣給出精確到個位數、看起來很篤定的價位。
- **沒有已知事件、或查不到相關資訊**：正常繼續下面的計算。

**5b. 計算進場價/停利/目標/停損**：對每一檔給下一交易日的五項（缺一不可）：進場價、停利點、目標價、停損點、策略說明。依前一日開高低收與振幅計算。若該股已經漲停鎖住，要特別標注追價風險。**這五項不是只有 `summary.recommendations` 裡的區間推薦才要寫成結構化數字，`candidates` 陣列裡每一檔也一樣要有 `entry`／`take_profit`／`target`／`stop_loss` 四個獨立欄位（見第 9 步 schema），不能只寫在 `plan_a` 的一大段文字裡——文字段落是給人看策略說明用的，但發布前健檢腳本只認得到結構化欄位，價位資訊如果只藏在文字裡，等於完全沒有防呆。**

**5c-0. 前 3 個交易日累積漲幅過高的獲利了結風險（2026-07-28 新增）**：查證候選股前 3 個交易日的累積漲跌幅。回測發現南亞科在 7/24 盤中衝到停利價出場，但尾盤獲利了結賣壓湧現、收盤大跌 4.1%——當時南亞科已經連續反彈多日、漲幅可觀。**累積漲幅過高（例如 3 個交易日內累積 ≥15%）的候選，要在 `risk_tag` 明確標注「近期累積漲幅已高，獲利了結風險偏高，若已達停利價應優先出場、不宜期待延續大漲」**，降低對「今天會延續同方向」的信心。

**5c. 前一日重挫股與加碼防呆（2026-07-23 回測後新增）**：
- 前一交易日跌幅達 -7% 以下的股票，隔日 09:15 前禁止做多；A 計畫必須明寫「09:15 後，站回開盤價、VWAP 與開盤 15 分鐘區間高點，回測 2–3 根一分鐘 K 不破才可進場」。不能把第一波急彈當止穩。
- 最近 3–5 分鐘若已急拉超過 2%，禁止直接追價；必須等待回測確認，且進場到最近壓力的空間至少是停損距離的兩倍。
- 未獲利至少 1R、且未完成突破回測前，禁止加碼。加碼後整筆交易的最大可能虧損不得增加。
- 每筆計畫必須提供結構化 `early_stop`（若無分批停損則為 `null`）、`risk_reward_ratio` 與 `invalidation_reason`，供回測器與前端直接驗證，不可只藏在自然語言裡。

**⚠️ 目標價／停利點常見錯誤（2026-07-16 真實事故，務必避免）**：曾經發生過目標價/停利點直接抄「前一交易日的最高價」，而不是根據「今天」真正的漲跌停區間重新計算——結果是目標價設得遠低於今天實際能到的漲停價（例如某檔今天漲停是 250 元，目標卻抄成昨天的最高價 230 元，白白少了 8.8% 的空間沒用到），而且停利點跟目標價擠在一起只差不到 1%，失去分層停利的意義。**正確做法**：
- 目標價的計算基準是「今天」用前收盤價（`前收 × 1.1` 概算，或更準確查證交所當天算好的官方漲停價）算出的今天漲跌停區間，不是「昨天」的高低點——昨天的高低點只能拿來判斷「這檔昨天強不強、振幅多大」，不能直接當成今天的目標價。
- 停利點跟目標價之間要留出合理級距（建議至少 1.5%-2%，不要擠在 1% 以內），兩者代表不同階段的分批出場，數字太接近就失去分層的意義。

**每檔都要給 A 計畫（拉回進場）／B 計畫（開盤沒拉回、直接站上前高噴出時的備案）**：
- **A 計畫**：三段式風控——進場區間 → 早期減碼點（跌破就先出一半）→ 最終停損（跌破就全出）。早期減碼點設在進場區間下緣與最終停損之間。
- **B 計畫**：突破追價用「站穩前高不破一段時間才追價＋收緊停損」的邏輯，單一收緊停損，不疊加二段式。
- 明確講清楚什麼情況下 A、B 計畫都不成立就直接放棄不做，寫進 `summary` 文字。

## 第 6 步：三層風險體檢（缺一不可，這是本流程最容易出錯也最重要的一步）

**a. 處置股／注意股體檢**
每一檔都要用 WebSearch 查是否被 TWSE/櫃買中心列為注意股或處置股（搜尋「[股票名稱] 處置股 注意股 [年月]」）。
- 處置股：改成人工撮合，等於無法做正常當沖 —— **直接排除**。
- 注意股：風險偏高但還能交易，要在該檔的 `risk_tag` 標注。
- 查不到明確資訊的股票，不要假設它是乾淨的——在 warnings 標注「[股票代號] 處置股狀態未能確認」，並在該檔的信心度上保守處理。

**b. 國際盤勢背景**
查當天/前一天美股（道瓊、那斯達克、標普）收盤，以及亞股（日經、KOSPI，尤其牽涉半導體/記憶體時查三星、SK 海力士）走勢。

**時差陷阱（務必檢查）**：美股開盤是台灣時間晚上 9:30、收盤是隔天凌晨 4 點左右。這份報告是台灣時間早上 8:30 產生，**這個時間點美股當晚那一場理論上已經收盤**（凌晨 4 點 vs 8:30，已經過了），所以正常情況下不會有時差問題。但仍然要做一件事：**查詢時明確確認你查到的美股數字是「收盤價」不是「盤中報價」**——查證時注意新聞或數據來源的時間戳記，如果查到的資訊時間戳落在台灣時間凌晨 4 點之前（代表還沒收盤），要換一個來源重新查，不要把盤中價當收盤價。查不到明確收盤數據時，在 warnings 標注「國際盤勢資料可能非最終收盤值」。

**ADR 跳空風向球（2026-07-16 新增，回測抓到的真實模式）**：2026-07-14 台股史上第 6 大單日盤中重挫那天，事後查證發現台積電 ADR（美股代號 TSM）早在台股開盤前（美股 7/13 收盤）就已經下跌 2.89%——這個數字在報告產生的 8:30 是已經查得到的公開資訊，卻沒被用上，等於錯過一個現成的跳空預警訊號。對安全牌/衝最快候選裡有美股 ADR 的股票（尤其台積電 TSM，其他大型權值股也可能有），額外用 WebSearch 查一次「[股票代號/名稱] ADR」當天收盤漲跌幅：
- ADR 顯示大跌（例如 ≥ 2%）：這檔當天開盤大機率會跳空低開，進場區間的精確度要視為偏低，`risk_tag` 明確標注「ADR 已下跌 X%，開盤跳空風險偏高」，並考慮調降信心度或縮小建議部位。
- ADR 顯示大漲：同理，標注跳空風險，但方向相反。
- 查不到該股 ADR 資料（多數中小型股沒有掛牌 ADR）：略過這項，不用勉強找。

**SOX 費城半導體指數（2026-07-28 新增，回測發現的第二次同類缺口）**：2026-07-27 緯創觸停損那天，事後查證發現起因是美股費城半導體指數（SOX）在前一交易日重挫 4.25%（Alphabet 上修 AI 資本支出引發的科技股賣壓外溢），但緯創本身沒有掛牌 ADR，前述「ADR 跳空風向球」規則接不住這種情況——這是繼 7/14 之後第二次因為沒查國際半導體類股情緒而漏接系統性外溢。**只要候選股屬於半導體/電子零組件/AI 供應鏈相關族群（不限於自己有掛牌 ADR），一律額外查一次美股前一交易日 SOX 指數收盤漲跌幅**：
- SOX 大跌（例如 ≥ 3%）：即使該股自己沒有 ADR、當天也沒有個股層級的利空，仍要在 `risk_tag` 標注「美股費半重挫 X%，類股情緒外溢風險偏高」，並調降信心度或縮小部位。
- 查不到 SOX 數據：在 warnings 誠實標注，不要略過不提。

**系統性崩盤修復期偵測（2026-07-28 新增）**：查證近 5 個交易日的大盤（加權指數）是否出現過單日跌幅 ≥5% 的異常日（例如回測抓到的「717 股災」單日跌 6.47%）。若有，代表市場可能仍處於去槓桿/崩盤修復期，這種環境下**個股層級的技術價位法本質上難以防禦系統性風險**——當天所有推薦（包含 `summary.recommendations` 裡每一區間的每一檔）整體調降信心度、縮小建議部位，並在 `summary` 文字明講「近期有系統性重挫，個股方法論有效性降低，建議降低部位」。

**c. 資金與流動性防呆**（2026-07-16 新增，2026-08-03 改為對齊使用者真實本金，2026-08-04 改為股價區間制；參考 CHARLES AGENT Firebase 那套系統踩過的坑補上）
- **每張成本上限＝使用者真實本金新台幣 50 萬**：算一下「進場價 × 1000 股」，如果超過 50 萬，這檔在第 8 步的股價區間分類裡自動落在「500 元以上・市場觀察」（因為 500 元進場價 × 1000 = 50 萬，剛好是門檻），**不算可操作推薦**，只是給使用者參考市場動態用。100-250 元／250-500 元這兩個可操作區間裡的股票，定義上單張成本一定在 50 萬以內，不需要另外做排除判斷。`candidates` 排行仍然可以放全部價位的股票（讓使用者看到市場熱點全貌），每一檔都要在 `risk_tag` 開頭明確標注「單張成本約 XX 萬，超過 50 萬本金上限」（適用時）——這不是可省略的軟性提醒，是必填。發布前 `scripts/validate-report.js` 會用 `進場價 × 1000 > 500000` 這個機器規則，自動幫每一檔（含 candidates 與第 8 步的區間推薦）標記 `affordable`/`lot_cost_ntd`/`capital_note`/`price_tier`/`market_observation_only` 結構化欄位，前端會依這些欄位分區塊渲染——生成階段的 `risk_tag` 標注只是第一道防線，真正生效的是這個機器驗證，兩邊口徑要一致，不要各說各話。
- **貼近漲停自動降級**：如果某檔當天漲幅已經接近或達到當日合理漲停區間（例如漲幅 ≥ 9%、或已經鎖漲停），**不能把它當作「拉回進場」的 A 計畫主推薦**，因為隔日追高的風險極高、也最容易變成下一波被列注意股/處置股的對象。這種股票只能留在 B 計畫（突破追價、單一收緊停損）當備案，`risk_tag` 要明確寫「貼近漲停，追價風險極高」。如果篩選到最後，「安全牌」或「衝最快」剛好只剩貼近漲停的標的可選、沒有更適合的候補，**寧可把結論誠實降級為「保守觀望」，也不要硬塞一檔貼近漲停的股票進正式推薦**。

## 第 5d 步：中繼檢查點——先 push 一版基本報告（2026-08-06 新增）

**背景**：8/4 下午把報告結構從固定 2 檔改成多區間彈性數量後，8/5、8/6 兩次早上的執行都在中途卡死、完全沒有 commit 出任何東西，使用者兩天都拿不到報告。無法確認雲端執行環境有沒有時間或回合數上限，但既然抓不到確切原因，**用「分階段交付」直接讓這個失敗模式不可能再發生**：就算後面真的又卡住，至少已經有一版基本報告在使用者手上，不會像現在這樣完全空手。

做完上面第 5 步（每一檔候選都有 entry/take_profit/target/stop_loss/plan_a/plan_b 這些基本欄位）之後：
1. 先寫一版 `data/latest.json`，用第 9 步的完整 schema，但 `sentiment`、`narrative_timeline`、`confidence_score`／`confidence_factors`、`news` 這些還沒做的欄位可以先給合理預設值（`sentiment.score` 給 50／`label` 給「中性（尚未完成深度分析）」；`news` 給空陣列；`confidence_score` 每檔先給 50）——**這些欄位第 6-9 步做完後會被覆蓋，這裡只是先求有，不用先求好**。
2. `data_quality.warnings` 加一條「快速檢查點版本：法人動向/國際盤勢/處置股體檢/新聞等深度分析尚未完成，稍後會更新完整版」，`provisional` 設 `true`。
3. 跑一次 `node scripts/validate-report.js`（此時只會驗基本欄位跟漲跌停/邏輯順序，不會擋在深度分析上），沒有 `✗ 錯誤` 就直接 `git add`／`git commit -m "Quick checkpoint for $(date +%Y-%m-%d)"`／`git push`。
4. **push 完這個檢查點才繼續往下做第 6-9 步的完整版**，做完後照第 11 步再 commit＋push 一次覆蓋掉檢查點（`provisional` 設回 `false`，訊息維持 `"Daily report for $(date +%Y-%m-%d)"`）。
5. **如果完整版做到一半明顯感覺時間或資源快耗盡**（例如已經對大半候選做完風險體檢，但新聞/sentiment 還沒做）：**把已經做完的部分寫進去，剩下的老實記進 `data_quality.warnings`，直接 commit push 現有進度**，不要因為想做到完美而完全不 commit——這正是造成 8/5、8/6 兩次空手的原因，不要重蹈覆轍。

## 第 7 步：新聞：濃縮重點＋多面向延伸分析

用 WebSearch 找當天相關新聞，**至少 6 則，並且盡量橫跨至少 4 個不同類別**（例如：總經/貨幣政策、產業趨勢、個股消息、國際情勢/地緣政治、法規政策），不要全部集中在同一個角度（例如清一色都是財經數字新聞）。每則給：濃縮重點、多面向延伸分析（不只股票影響，也要有國際趨勢/地緣政治/總經角度），並且在 JSON 的 `category` 欄位標注這則新聞屬於哪個類別（方便之後檢查多樣性）。

## 第 8 步：報告結構（2026-08-04 改為股價區間分組，不再固定湊 2 檔）

**不要再套「安全牌+衝最快各選 1 檔」這種固定 2 格的舊規則。** 改成依「進場價」把每一檔候選分到四個股價區間：100 元以下／100-250 元／250-500 元／500 元以上。**100-250 元跟 250-500 元這兩個區間是「可操作」區間**，每個區間依你分析後真正覺得夠格的品質，產出 0 檔以上的推薦——夠格幾檔就列幾檔，不要為了湊數硬推薦不夠格的標的，也不要因為同一區間已經有一檔「安全牌」型態就放棄推薦另一檔真正夠格的「衝最快」型態（同一區間可以同時有多檔安全牌、多檔衝最快，或該區間今天剛好一檔都不夠格）。每一檔仍然要標示類型（`type`: `"safe"` 安全牌或 `"aggressive"` 衝最快）、完整 A/B 計畫與所有結構化欄位（entry/target/stop_loss 等，見第 9 步 schema），挑選時一樣要先過處置股/注意股體檢。

**500 元以上區間是「市場觀察」，不是可操作建議**——因為一張（1000 股）進場成本一定超過使用者 50 萬本金上限，使用者實際下不了單，這個區間存在的目的純粹是讓使用者了解大型股/高價股當天在做什麼，不是要你幫他規劃買不起的交易。這個區間一樣可以列出當天值得關注的幾檔（0 檔以上），欄位結構跟其他區間一致（不用另外設計簡化 schema），但發布前 `scripts/validate-report.js` 會依進場價自動判定 `price_tier`／`market_observation_only` 這兩個欄位並覆蓋寫回，你不用自己算、算錯也沒關係，機器會校正——但為了跟後製結果口徑一致，你自己產生內容時心裡也要用「進場價 × 1000 是否 ≥ 50 萬」這個判斷基準。

**100 元以下**的股票如果剛好出現在候選池裡，一樣可以分析推薦，正常算可操作區間（比 100-250 更便宜、更買得起）。

若第 6 步的風險體檢顯示風險偏高，個別區間的結論要誠實降級為「保守觀望、這區間今天沒有夠格的推薦」，不要為了給信心而硬撐高信心度或硬湊檔數。

## 第 8b 步：信心度、多空情緒分數、產業敘事時間軸（2026-07-28 新增，前端終端機改版要用的新欄位）

這三個是新的結構化欄位，**都要有明確可攤開檢查的算法，不能是憑感覺喊一個數字**——這是為了避免重蹈 `backtest.js` 之前「模板化空話」的覆轍。

**8b-1. `confidence_score`（`summary.recommendations` 裡每一檔各自一個，0-100 整數）**：
- 起始分數 70。
- 每有一項顯著風險（前一日跌幅≥7%的09:15限制、近期法說會、處置股/注意股疑慮未100%排除、資金/流動性警示、資料矛盾）：扣 8-12 分。
- ADR/SOX 訊號跟這檔的方向判斷一致（例如做多且 ADR/SOX 也偏多）：加 5-10 分；訊號矛盾則扣 5-10 分。
- 第6步系統性崩盤修復期偵測若觸發：全部候選再扣 10-15 分。
- 最終夾在 20-95 之間（不給滿分也不給趨近0分，避免虛假的絕對確定或絕對否定）。
- 在該檔位底下，額外用一個陣列 `confidence_factors` 列出加減分明細（例如 `["基礎70分", "法說會風險 -10", "SOX方向一致 +8"]`），前端會直接顯示這個明細，不能只給最終數字。

**8b-2. `sentiment`（整份報告一個，不分檔位）**：
- 起始分數 50（中性）。
- 國際盤勢（第6b步查到的美股/SOX）偏多：+10~15；偏空：-10~15；持平：不加減。
- 類股當日/近期消息面（法人喊漲、產業消息、供需報告等）偏多：+15~20；偏空：-15~20。
- 第6步系統性崩盤修復期偵測觸發：-25~30（大幅下修，因為此時任何樂觀判斷都該打折）。
- 最終夾在 0-100，並依分數對應標籤：0-25「空頭」、26-45「偏空」、46-55「中性」、56-75「中性偏多」、76-100「多頭」（分數與標籤都要輸出）。
- 額外輸出 `factors` 陣列（每項 `{"type": "bull"|"bear"|"flat", "text": "一句話說明"}`），至少 3 條，對應國際盤勢／類股情緒／系統性風險三個面向，這是前端「今日多空儀表板」下面顯示的依據清單，不能空著。**這個欄位現在也會被直接推播到 LINE 給使用者看，內容裡的具體數字/因果論述要照第 7 行「雙來源查證原則」處理，不能只憑一篇文章就寫成定論。**

**8b-3. `narrative_timeline`（產業敘事時間軸，全球總體動態追蹤——這是持續累積更新的，不是每天重寫）**：
- 這是回測發現「717股災」「兆易創新示警」「Alphabet資本支出外溢」這類真實因果鏈之後，希望能每天持續追蹤更新的功能，**涵蓋全球主要經濟體**（中國、美國、南韓、台灣本地等）任何對候選股所屬產業造成實際影響的消息，不限於單一國家。
- **執行方式**：先讀取這次 git checkout 下來、還沒被覆寫的舊版 `data/latest.json`（也就是前一個交易日的版本），取出其中的 `narrative_timeline` 陣列（如果不存在或讀取失敗，視為空陣列，不要因此中斷流程）。
- 用 WebSearch 查證「今天／最近一個交易日，候選股所屬產業（例如記憶體、AI伺服器、半導體、塑化等）有沒有新的重大消息（漲價/跌價、大廠報價、法人喊話、政策、地緣政治）」，找到具體、可查證、有來源的一則新發展（不要為了填滿而硬湊，查無明確新發展時，這次可以不新增節點，只延續舊的時間軸）。
- 把新查到的節點（`{"date": "YYYY-MM-DD", "text": "一句話敘述，具體到誰說了什麼/發生什麼", "current": true}`）加到陣列最後，並把前面所有舊節點的 `current` 都改成 `false`（只有最新一則是 `current: true`）。
- **陣列最多保留最近 6 則**，超過就把最舊的丟掉（`shift()`），維持時間軸精簡好讀。
- 每則的 `text` 要具體到可查證的程度（誰、什麼消息、影響），不要寫空泛的「市場情緒轉變」這種話，要跟 7/16、7/28 那兩份回測報告裡查到的因果分析同等具體程度。**這個欄位現在也會被直接推播到 LINE 給使用者看，一樣要照第 7 行「雙來源查證原則」交叉查證，不能單一來源就寫成確定事實。**

## 第 9 步：輸出格式

把結果寫成這個 JSON 結構，覆寫 `data/latest.json`（**注意：欄位名稱要完全照這個格式，前端 `index.html` 是照這個格式 render 的**）：

**`summary.recommendations` 是陣列，長度不固定**（2026-08-04 起不再是固定的 `safe_pick`/`aggressive_pick` 兩個欄位）——依第 8 步的規則，100-250／250-500／500 以上三個區間各自放進 0 檔以上真正夠格的推薦，每筆都要有 `type`（`"safe"` 或 `"aggressive"`）欄位。你不用自己在資料裡標 `price_tier`/`market_observation_only`，`scripts/validate-report.js` 會依 `entry` 價自動算出來並覆蓋寫回，就算你沒填或填錯也會被機器校正。

```json
{
  "schema_version": 3,
  "date": "2026-07-09",
  "generated_at": "2026-07-09 08:30",
  "provisional": false,
  "market_open": true,
  "data_quality": {
    "warnings": [
      "任何第 1/6 步發現的資料不完整都列在這裡，沒有問題就給空陣列"
    ]
  },
  "sentiment": {
    "score": 62,
    "label": "中性偏多（0-25空頭/26-45偏空/46-55中性/56-75中性偏多/76-100多頭）",
    "factors": [
      { "type": "flat", "text": "國際盤勢：一句話說明" },
      { "type": "bull", "text": "類股情緒：一句話說明" },
      { "type": "bear", "text": "系統性風險：一句話說明" }
    ]
  },
  "narrative_timeline": [
    { "date": "2026-07-27", "text": "具體、可查證的事件敘述", "current": true }
  ],
  "summary": {
    "recommendations": [
      {
        "symbol": "2330",
        "name": "台積電",
        "type": "safe",
        "reason": "一句話理由",
        "entry": "進場價",
        "take_profit": "停利點",
        "target": "目標價",
        "stop_loss": "停損點",
        "early_stop": "第一段減碼價；沒有則填 null",
        "risk_reward_ratio": "例如 2.0",
        "invalidation_reason": "什麼情況代表策略失效",
        "prior_day_change_pct": "前一交易日漲跌幅，例如 -8.5",
        "risk_tag": "選填，例如「注意股」",
        "confidence_score": 72,
        "confidence_factors": ["基礎70分", "法說會風險 -10", "SOX方向一致 +8", "..."]
      }
    ]
  },
  "candidates": [
    {
      "rank": 1,
      "symbol": "2330",
      "name": "台積電",
      "category": "所屬族群，例如「晶圓代工」",
      "summary": "完整策略說明文字",
      "entry": "進場價（跟 plan_a 文字裡的區間一致，不是選填）",
      "take_profit": "停利點（跟 plan_a 文字裡的數字一致，不是選填）",
      "target": "目標價（跟 plan_a 文字裡的數字一致，不是選填；必須根據今天的漲跌停區間算，不能抄前一天的高低點）",
      "stop_loss": "停損點（跟 plan_a 文字裡的數字一致，不是選填）",
      "early_stop": "第一段減碼價；沒有分批停損則填 null",
      "risk_reward_ratio": "依進場區中點、停損與第一停利計算的數值",
      "invalidation_reason": "價格或市場條件失效的明確敘述",
      "prior_day_change_pct": "前一交易日漲跌幅，例如 -8.5",
      "plan_a": "A 計畫文字（含三段式風控）",
      "plan_b": "B 計畫文字",
      "risk_tag": "選填"
    }
  ],
  "news": [
    { "title": "新聞標題", "summary": "濃縮重點＋延伸分析文字", "category": "總經／產業／個股／國際情勢／政策法規 擇一" }
  ]
}
```

## 第 10 步：發布前健檢（自動化，不是人工複核）

寫完 `data/latest.json` 之後、commit 之前，一定要先跑：

```bash
node scripts/validate-report.js
```

這支腳本會拿每一檔的進場/停利/目標/停損價，跟證交所當天官方算好的漲停/跌停價比對，抓「漲停算錯」「價格區間塞錯」這類會直接誤導交易決策的錯誤（2026-07-15 CHARLES AGENT Firebase 那套系統就出過這種錯，這裡是為了不要重蹈覆轍才加的）。

- **結尾印出「退出碼: 1」或看到 `✗ 錯誤` 那個區塊**：代表抓到真的問題，回頭修正 `latest.json` 裡對應的數字（通常是算錯或抄錯），改完重新跑一次這支腳本，直到沒有 `✗` 為止，才可以往下 commit。不要略過這一步直接 push。
- **只有 `⚠ 警告`、沒有 `✗ 錯誤`**：可以放行，但把警告內容一併記進 `data_quality.warnings`，誠實標注不要略過。
- 這支腳本本身需要連網查即時股價（呼叫證交所公開 API），如果連線失敗查不到某檔的前收盤價，會印警告但不會擋發布——這種情況下改成靠人工複核那幾檔的漲跌停有沒有算對。
- **重試上限（2026-08-06 新增，懷疑是 8/5 報告完全沒有產出的根因）：同一檔最多修正 2 次、總共跑 3 次驗證腳本。第 3 次還是有 `✗ 錯誤`：把那一檔整個從 `summary.recommendations`／`candidates` 移除，不要繼續在同一檔上原地打轉修下去。** 如果移除到最後 `summary.recommendations` 剩 0 檔，那是合法結果（第 8 步已經說明可以是 0 檔），不是你沒做完，正常寫進 `data_quality.warnings` 誠實說明「[股票代號] 因驗證持續失敗已移除」，然後照樣往下走完第 11 步 commit＋push。**任何情況下都要在合理時間內走到 commit 這一步，寧可交出一份「拿掉幾檔問題股但準時送達」的報告，也不要為了追求零錯誤而完全沒有報告可看。**

## 第 11 步：commit 並 push

```bash
git config user.name "daily-report-bot"
git config user.email "daily-report-bot@users.noreply.github.com"
git add data/latest.json
git commit -m "Daily report for $(date +%Y-%m-%d)"
git push
```

如果 push 失敗（例如遠端有其他 commit），先 `git pull --rebase` 再 push 一次。

## 交付前最後檢查

- **第 0 步做了沒？今天真的是正常交易日嗎？**（休市卻照樣生出進場價，是 2026-07-10 真實發生過的事故）
- 有沒有把盤中報價當收盤價？
- 每一檔選股是否都做過處置股/注意股查證？
- TAIFEX 成交量排行是否確認頁數/筆數完整？
- `summary.recommendations` 裡 100-250／250-500 這兩個可操作區間的每一檔，有沒有做過貼近漲停降級這條規則？500 元以上區間有沒有正確標成市場觀察、沒有跟可操作推薦混在一起？
- 前一日跌幅達 -7% 以下的股票，是否明確套用 09:15 前禁止做多與回測確認規則？
- `early_stop`、`risk_reward_ratio`、`invalidation_reason` 是否都是結構化欄位，而不是只寫在文字裡？
- 有沒有查過候選股近期的法說會/重大事件？有沒有查過主要候選（尤其台積電）的 ADR 走勢？半導體/電子/AI供應鏈候選有沒有另外查 SOX 費半指數？
- 有沒有查過近 5 個交易日大盤是否有單日跌幅 ≥5% 的系統性重挫日？有的話是否已整體調降信心度？
- 有沒有查過候選股前 3 個交易日累積漲幅？過高的是否已標注獲利了結風險？
- 如果 TAIFEX/TWSE 全部 403：候選池是否有拼到至少 8-12 檔，而不是隨便湊 3-5 檔就收工？
- `confidence_score`／`sentiment.score` 是否都照第 8b 步的加減分規則算出來、附上明細，不是憑感覺喊的數字？
- `narrative_timeline` 是否讀取了前一份 `latest.json` 的舊節點並延續（不是每天重寫整條時間軸），且陣列不超過 6 則？
- 新聞是否至少 6 則、橫跨至少 4 個類別？
- `node scripts/validate-report.js` 有沒有跑過、而且沒有 `✗ 錯誤`？
- 以上任何一項有疑慮，`data_quality.warnings` 是否都誠實列出來了？

檢查完直接 commit push，不用回報過程。
